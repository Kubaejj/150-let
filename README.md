# 150-let

**Rychlý start**

- Klonujte repozitář nebo rozbalte projekt do adresáře.
- Nainstalujte vývojové závislosti (PowerShell):
- Spuštění vývoje (spustí watch a další úlohy):

```powershell
gulp
```


**Struktura (základní)**

- `src/` – zdrojové soubory (HTML šablony, `styles/`, `scripts/`, `img/`)
- `styles/`, `scripts/`, `img/` – výstupy prohlížeče (do kořene projektu)
